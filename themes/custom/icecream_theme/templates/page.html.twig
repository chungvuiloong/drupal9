{# <div id="header">{{ page.header }}</div>

<div id="body">{{ page.body }}</div>

<div id="menu">{{ page.menu }}</div>
<div id="content">{{ page.content }}</div>

<div id="sidebar">{{ page.sidebar }}</div>

<div id="pokemon">{{ page.pokemon }}</div>

<div id="wtf">{{ page.wtf }}</div>

<div id="footer">{{ page.footer }}</div> #}

<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
    </head>
    <body>
        <header class="header"> {{ page.header }}</header>
        <div id="heading"><h1>Welcome</h1></div>
        <nav class="menu">{{page.menu}}</nav>
        <main class="main">
            <div class="content">
                {{ page.content}}
            </div>
            {% if page.sidebar %}
            <div class="sidebar">{{ page.sidebar }}</div>
            {% endif %}
        
        </main>
        <footer class="footer">{{ page.footer }}</footer>    
    </body>
</html>